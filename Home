<!DOCTYPE html>
<html lang="en" >
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Local Card Shop</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap');

    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background: linear-gradient(135deg, #8B5CF6 0%, #EC4899 100%);
      color: #fff;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      text-align: center;
      padding: 20px;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 0.5rem;
      text-shadow: 2px 2px 6px rgba(0,0,0,0.3);
    }

    p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.3);
    }

    .button-container {
      display: flex;
      gap: 20px;
      flex-wrap: wrap;
      justify-content: center;
      width: 100%;
      max-width: 600px;
    }

    .btn {
      background: rgba(255,255,255,0.15);
      border: 2px solid #fff;
      border-radius: 12px;
      padding: 20px 40px;
      font-size: 1.5rem;
      color: white;
      cursor: pointer;
      flex: 1 1 150px;
      max-width: 180px;
      transition: background 0.3s, box-shadow 0.3s;
      box-shadow: 0 0 10px rgba(255,255,255,0.2);
      user-select: none;
    }

    .btn:hover {
      background: #fff;
      color: #8B5CF6;
      box-shadow: 0 0 15px #fff;
      border-color: #8B5CF6;
    }

  </style>
</head>
<body>

  <h1>Your Local Card Shop</h1>
  <p>Choose a sport to enter the store</p>

  <div class="button-container">
    <button class="btn" onclick="enterStore('baseball')">Baseball</button>
    <button class="btn" onclick="enterStore('football')">Football</button>
    <button class="btn" onclick="enterStore('basketball')">Basketball</button>
  </div>

  <script>
    function enterStore(sport) {
      // For now, just redirect to sport page placeholders
      window.location.href = `${sport}.html`;
    }
  </script>

</body>
</html>
